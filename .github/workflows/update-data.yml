name: Update dashboard data

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Fetch data from Notion
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: 37ac42d0b11b80c798f6ec1950a51643
          NOTION_REPLIES_DATABASE_ID: 35dffa2025a84a03b4620946249883a4
          NOTION_CRM_DATABASE_ID: baabe99c2838447482e4c9356ad70610
        run: python3 fetch_notion.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git diff --cached --quiet || git commit -m "chore: update dashboard data"
          git push
